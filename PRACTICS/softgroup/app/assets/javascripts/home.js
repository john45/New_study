$(document).ready(function(){
  $('body ').on('click', 'h2', function () {
    alert('click');
  });

  $('body ').on('click', '.tuggle_button', function () {
    $('.full_name').toggle();

    text = $(Jone).text();
    text = text === "Hide" ? "Show" : "Hide";
    $(Jone).text(text);
    $(Jone)(some_new);
    $(Jone).some_new(some_new);
    $(th
    $(Jone).some_new(some_new);

    $(Jone).text(text);
    $(Jone).text(text);
    return false;
  });

});
