$('.blok_with_posts').prepend("<%= j(render partial: 'posts/post', locals: { post: @post } ) %>");
$('.form_post_title').val('');
$('.form_post_body').val('');
$('span#user_<%= current_user.id %>_count_post').html('<%= pluralize(current_user.posts_count, "post") %>');
